# Ragas testset dataset
Classes and functions for generating custom testset using ragas.
## Generate Knowledge Graph
A knowledge graph from documents in S3 bucket can be generated

python generate_kg.py --config kg_config.yaml

## Generate testset
From the knowledge graph, we can generate a testset dataset